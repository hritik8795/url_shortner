version: '3.9'

services:

  api:
    build: .
    ports:
      - "8000:8000"

    depends_on:
      - db

  db:
    image: postgress

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: url_db

    ports:
      - "5432:5432"